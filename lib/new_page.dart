import 'package:flutter/material.dart';
import 'db_helper.dart';

class NewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('New Page BBBB'),
      ),
      body: Center(
        child: Text('This is a new page VVVVVVV'),
      ),
    );
  }
}
